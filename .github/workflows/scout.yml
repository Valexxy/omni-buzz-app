name: 🛰️ OMNI-BUZZ GEO-SCOUT
on:
  schedule:
    - cron: '*/10 * * * *' # Runs every 10 mins
  workflow_dispatch: # Allows you to run it manually from GitHub

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install Dependencies
        run: pip install requests supabase groq
      - name: Run Scout
        env:
          SUPABASE_URL: ${{https://yawqzipdrqbfagxfdehn.supabase.co}}
          SUPABASE_KEY: ${{sb_publishable_Z-ABrrS87ypwUa7I8ykehQ_ePvodWD7}}
          GROQ_API_KEY: ${{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}}
          UNSPLASH_KEY: ${{BNdQDshc6_QA7WpVYjRIbLd4Jem1JeMlCjrA8vE0e_M}}
        run: python image_scout.py
